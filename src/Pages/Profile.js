const Profile = () => {
  return (
    <div
      style={{
        display: "flex",
        aglinItems: "center",
        justifyContent: "center",
      }}
    >
      Profile page
    </div>
  );
};

export default Profile;
